import React from 'react';
import Nav from '../components/Nav';
import Store from '../components/Store';

const Home = () => {
    return (
        <section>
            <Nav/>
            <Store/>
        </section>
    );
};

export default Home;